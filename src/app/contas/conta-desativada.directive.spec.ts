import { ContaDesativadaDirective } from './conta-desativada.directive';

describe('ContaDesativadaDirective', () => {
  it('should create an instance', () => {
    const directive = new ContaDesativadaDirective();
    expect(directive).toBeTruthy();
  });
});
